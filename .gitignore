# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Vercel
.vercel

# Extensão do Chrome: as credenciais são geradas do .env pelo npm run extensao.
# (extensao/dist/ já é pego pela regra `dist` acima.)
extensao/lib/config.gerado.js

# Local env files — a exceção do .env.example precisa vir por último,
# senão um `.env*` posterior volta a ignorá-lo.
.env
.env.*
.env*
!.env.example
